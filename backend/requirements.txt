# ─── Web framework ───
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# ─── Auth ───
pyjwt[crypto]==2.10.1          # verify Supabase JWTs (HS256 now; JWKS-ready)

# ─── OpenAI & LLM orchestration (pinned to versions proven in langchain_env) ───
openai==1.84.0
langchain==0.3.21
langchain-core==0.3.65
langchain-openai==0.3.22
langchain-community==0.3.20
langgraph==0.3.16
langgraph-prebuilt==0.1.3
llama-index-core==0.12.40
llama-index-llms-openai==0.4.3
llama-index-embeddings-openai==0.3.1   # NLSQLTableQueryEngine resolves a default embed model at init

# ─── Database (Supabase Postgres) ───
sqlalchemy==2.0.39
psycopg2-binary==2.9.10

# ─── Data handling ───
pandas==2.2.3
numpy==1.26.4

# ─── Monitoring ───
sentry-sdk[fastapi]==2.20.0    # error monitoring (active only when SENTRY_DSN set)

# ─── Observability (Phase 3): OpenTelemetry traces/metrics/logs over OTLP ───
# SDK 1.43.0 pairs with instrumentation 0.64b0 (contrib versioning: 1.x ↔ 0.(x+21)b0).
# Active only when OTEL_EXPORTER_OTLP_ENDPOINT is set (see app/telemetry.py).
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-http==1.43.0
opentelemetry-instrumentation-fastapi==0.64b0
opentelemetry-instrumentation-sqlalchemy==0.64b0
opentelemetry-instrumentation-httpx==0.64b0

# ─── LLM observability (Phase 3, M5): Langfuse v4 (needs Python ≥3.11) ───
# Active only when LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY are set.
langfuse==4.13.0

# ─── Dev / test ───
pytest==8.3.4
httpx==0.28.1                  # FastAPI TestClient
